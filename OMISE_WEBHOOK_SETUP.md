# การตั้งค่า Omise Webhook

เอกสารนี้จะอธิบายวิธีการตั้งค่า Webhook ของ Omise เพื่อให้ระบบรับการแจ้งเตือนจาก Omise เมื่อมีการเปลี่ยนแปลงสถานะการชำระเงินโดยอัตโนมัติ

## 📋 สารบัญ

1. [ข้อดีของการใช้ Webhook](#ข้อดีของการใช้-webhook)
2. [การตั้งค่าใน Omise Dashboard](#การตั้งค่าใน-omise-dashboard)
3. [การตั้งค่า Environment Variables](#การตั้งค่า-environment-variables)
4. [การทดสอบ Webhook](#การทดสอบ-webhook)
5. [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## ข้อดีของการใช้ Webhook

### 🔄 ระบบปัจจุบัน (Polling)
- ต้องเรียก API `/api/payments/status` เป็นระยะๆ
- ใช้ทรัพยากร server และ network มากขึ้น
- อาจมีการ delay ในการอัพเดทสถานะ

### ⚡ ระบบ Webhook
- Omise จะส่งการแจ้งเตือนทันทีเมื่อมีการเปลี่ยนแปลงสถานะ
- ใช้ทรัพยากรน้อยลง
- อัพเดทสถานะแบบ real-time
- ไม่ต้องรอ polling interval

---

## การตั้งค่าใน Omise Dashboard

### ขั้นตอนที่ 1: เข้าสู่ Omise Dashboard

1. เข้าไปที่ [https://dashboard.omise.co](https://dashboard.omise.co)
2. Login เข้าสู่ระบบ
3. เลือก Account ที่ต้องการตั้งค่า

### ขั้นตอนที่ 2: เปิดหน้า Webhooks Settings

1. ไปที่เมนู **Settings** → **Webhooks** (หรือ **Developers** → **Webhooks**)
2. คลิกปุ่ม **Add Webhook** หรือ **Create New Webhook**

### ขั้นตอนที่ 3: กำหนดค่า Webhook

#### สำหรับ Development (Test Mode)

**Webhook URL:**
```
https://your-domain.vercel.app/api/webhooks/omise
```

**หรือสำหรับ Local Development (ใช้ ngrok):**
```
https://your-ngrok-url.ngrok.io/api/webhooks/omise
```

#### สำหรับ Production (Live Mode)

**Webhook URL:**
```
https://your-production-domain.com/api/webhooks/omise
```

### ขั้นตอนที่ 4: เลือก Events ที่ต้องการรับ

เลือก Events ต่อไปนี้:

- ✅ **charge.complete** - เมื่อการชำระเงินสำเร็จ
- ✅ **charge.expire** - เมื่อ QR Code หมดอายุ
- ⚪ **charge.create** - เมื่อสร้าง Charge ใหม่ (optional)

> **หมายเหตุ:** Event `charge.complete` เป็น event ที่สำคัญที่สุด ระบบจะใช้ event นี้ในการอัพเดทสถานะ Order เป็น "paid" และ generate keys อัตโนมัติ

### ขั้นตอนที่ 5: บันทึก Webhook

1. คลิกปุ่ม **Save** หรือ **Create Webhook**
2. Omise จะแสดง Webhook ID และ Secret Key (ถ้ามี)

---

## การตั้งค่า Environment Variables

ตรวจสอบให้แน่ใจว่ามี Environment Variables ต่อไปนี้:

```env
# Omise Keys (ต้องใช้ Secret Key สำหรับ webhook signature verification)
OMISE_PUBLIC_KEY="pkey_test_xxxxxxxxxxxxx"  # สำหรับ Test Mode
OMISE_SECRET_KEY="skey_test_xxxxxxxxxxxxx"  # สำหรับ Test Mode

# สำหรับ Production
# OMISE_PUBLIC_KEY="pkey_live_xxxxxxxxxxxxx"
# OMISE_SECRET_KEY="skey_live_xxxxxxxxxxxxx"
```

### สำหรับ Vercel Deployment

1. ไปที่ Vercel Dashboard → Project → Settings → Environment Variables
2. เพิ่ม Variables:
   - `OMISE_PUBLIC_KEY`
   - `OMISE_SECRET_KEY`
3. Deploy ใหม่เพื่อให้ environment variables มีผล

---

## การทดสอบ Webhook

### วิธีที่ 1: ใช้ Omise Dashboard

1. ไปที่หน้า **Webhooks** ใน Omise Dashboard
2. คลิกที่ Webhook ที่สร้างไว้
3. คลิกปุ่ม **Send Test Event** หรือ **Test Webhook**
4. เลือก Event Type (เช่น `charge.complete`)
5. ตรวจสอบว่าได้รับ Response 200 OK

### วิธีที่ 2: ทดสอบด้วยการชำระเงินจริง

1. สร้าง Order ใหม่ในระบบ
2. สร้าง Payment และแสดง QR Code
3. สแกน QR Code และชำระเงิน
4. ตรวจสอบ Logs ใน Vercel Dashboard หรือ Server Logs
5. ตรวจสอบว่า Order Status เปลี่ยนเป็น "paid" อัตโนมัติ

### วิธีที่ 3: ใช้ Omise CLI (Optional)

```bash
# Install Omise CLI
npm install -g omise-cli

# Send test webhook event
omise events:create \
  --key charge.complete \
  --data '{"id":"chrg_test_xxxxx","paid":true,"status":"successful"}'
```

### ตรวจสอบ Logs

ดู Logs ใน:
- **Vercel Dashboard** → Project → Logs
- **Server Console** (ถ้า run local)
- **Omise Dashboard** → Webhooks → [Your Webhook] → Events

คุณควรเห็น Log ว่า:
```
Received Omise webhook event: charge.complete
Order {orderId} marked as paid via webhook
Generated {count} keys for order {orderId}
```

---

## การแก้ไขปัญหา

### ❌ ปัญหา: Webhook ไม่ทำงาน

**ตรวจสอบ:**
1. ✅ Webhook URL ถูกต้องและสามารถเข้าถึงได้ (ใช้ HTTPS)
2. ✅ Environment Variable `OMISE_SECRET_KEY` ตั้งค่าถูกต้อง
3. ✅ Webhook Signature Verification ผ่าน (ตรวจสอบ Logs)
4. ✅ Events ที่เลือกใน Omise Dashboard ตรงกับที่ต้องการ

### ❌ ปัญหา: Signature Verification Failed

**สาเหตุ:**
- `OMISE_SECRET_KEY` ไม่ถูกต้อง
- Secret Key ที่ใช้ไม่ตรงกับ Account/Mode (Test vs Live)

**แก้ไข:**
1. ตรวจสอบว่าใช้ Secret Key ที่ถูกต้อง
2. สำหรับ Test Mode ใช้ `skey_test_...`
3. สำหรับ Live Mode ใช้ `skey_live_...`

### ❌ ปัญหา: Order Status ไม่อัพเดท

**ตรวจสอบ:**
1. ✅ Webhook Event `charge.complete` ถูกส่งมา
2. ✅ Charge Status เป็น `successful` และ `paid = true`
3. ✅ Order มี `omiseChargeId` ที่ตรงกับ Charge ID จาก Webhook
4. ✅ ตรวจสอบ Database ว่า Order อัพเดทหรือไม่

### ❌ ปัญหา: Webhook ทำงานแต่ Keys ไม่ถูก Generate

**ตรวจสอบ:**
1. ✅ Order Status เปลี่ยนเป็น "paid" แล้ว
2. ✅ Product Type เป็น "key"
3. ✅ ตรวจสอบ Logs ว่ามี Error ในการ Generate Keys หรือไม่
4. ✅ ตรวจสอบว่า Order Item มี Quantity > 0

---

## Security Best Practices

### ✅ Signature Verification

ระบบจะตรวจสอบ Webhook Signature จาก Omise เพื่อป้องกันการส่งข้อมูลปลอม:

```typescript
// Signature Format: sha256=hexdigest
X-Omise-Signature: sha256=abc123...
```

### ✅ HTTPS Only

Webhook URL ต้องใช้ HTTPS เท่านั้น (Omise จะไม่ส่ง Webhook ไปที่ HTTP)

### ✅ Error Handling

- ระบบจะ log errors ทั้งหมด
- Return 200 OK แม้จะมี error เพื่อป้องกัน Omise ส่งซ้ำ
- ตรวจสอบ Logs เป็นประจำ

---

## Events ที่รองรับ

### ✅ charge.complete

เมื่อการชำระเงินสำเร็จ:
- อัพเดท Order Status → `paid`
- Generate Keys สำหรับ Key Products
- Update Stock สำหรับ Non-Key Products

### ✅ charge.expire

เมื่อ QR Code หมดอายุ:
- อัพเดท Order Status → `cancelled` (ถ้ายัง pending)

### ⚪ charge.create

เมื่อสร้าง Charge ใหม่:
- Log event (ไม่มี action อื่น)

---

## ระบบทำงานแบบ Hybrid

ระบบถูกออกแบบให้ทำงานแบบ **Hybrid** คือ:

### ⚡ Primary: Webhook (Real-time)
- Omise ส่ง notification ทันทีเมื่อมีการชำระเงิน
- ระบบอัพเดท Order Status อัตโนมัติภายในไม่กี่วินาที
- Keys ถูก Generate อัตโนมัติทันที
- **ไม่ใช้ทรัพยากร server ในการ polling**

### 🔄 Fallback: Polling
- ระบบยังคงใช้ Polling เป็น backup mechanism
- Polling interval เพิ่มเป็น **10 วินาที** (แทน 3 วินาที)
- ทำงานเมื่อ webhook ไม่ทำงานหรือมีปัญหา
- ตรวจสอบสถานะจาก Omise API โดยตรง

### ✅ ข้อดี
- **Real-time updates** จาก webhook
- **Reliable** ด้วย polling fallback
- **ประหยัดทรัพยากร** เพราะ polling ช้าลง
- **No single point of failure**

## สรุป

หลังจากตั้งค่า Webhook แล้ว:

1. ✅ Omise จะส่ง notification ทันทีเมื่อมีการชำระเงิน
2. ✅ ระบบจะอัพเดท Order Status อัตโนมัติ
3. ✅ Keys จะถูก Generate อัตโนมัติสำหรับ Key Products
4. ✅ Polling ยังคงทำงานเป็น fallback (ทุก 10 วินาที)
5. ✅ ระบบมีความน่าเชื่อถือสูงด้วย dual mechanism

---

## 📞 ต้องการความช่วยเหลือ?

- [Omise Documentation](https://www.omise.co/docs)
- [Omise Webhook Guide](https://www.omise.co/webhooks)
- [Omise Support](https://www.omise.co/contact)


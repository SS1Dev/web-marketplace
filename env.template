# Database
# สำหรับ MongoDB
# Local MongoDB: mongodb://localhost:27017/marketplace
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/marketplace?retryWrites=true&w=majority
DATABASE_URL="mongodb://localhost:27017/marketplace"

# NextAuth
# สร้าง Secret Key ด้วยคำสั่ง: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-key-here"

# Discord OAuth (Optional)
# สร้าง Discord Application ที่ https://discord.com/developers/applications
# ตั้งค่า Redirect URI: http://localhost:3000/api/auth/callback/discord
DISCORD_CLIENT_ID="your-discord-client-id"
DISCORD_CLIENT_SECRET="your-discord-client-secret"

# Omise Payment
# ลงทะเบียนที่ https://www.omise.co/
# ใช้ Test Keys สำหรับ Development
# ใช้ Live Keys สำหรับ Production
OMISE_PUBLIC_KEY="pkey_test_xxxxxxxxxxxxx"
OMISE_SECRET_KEY="skey_test_xxxxxxxxxxxxx"

# สำหรับ Production
# OMISE_PUBLIC_KEY="pkey_live_xxxxxxxxxxxxx"
# OMISE_SECRET_KEY="skey_live_xxxxxxxxxxxxx"

# GitHub (Optional - ใช้สำหรับดึงซอร์สโค้ดจาก GitHub ใน API /api/keys/verify)
# ถ้าไม่ตั้งค่า ระบบจะยังทำงานได้ แต่จะไม่ส่ง Authorization header ตอนเรียก GitHub
GITHUB_TOKEN="your-github-personal-access-token"

# ========================================
# สำหรับการ Deploy บน Vercel
# ========================================
# 1. ตั้งค่า Environment Variables ใน Vercel Dashboard:
#    Project Settings > Environment Variables
#
# 2. สำหรับ NEXTAUTH_URL ให้ใช้ URL ที่ได้จาก Vercel:
#    NEXTAUTH_URL="https://your-project.vercel.app"
#
# 3. สำหรับ DATABASE_URL แนะนำใช้ MongoDB Atlas:
#    DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/marketplace?retryWrites=true&w=majority"
#
# 4. สำหรับ Discord OAuth ต้องตั้งค่า Redirect URI เป็น:
#    https://your-project.vercel.app/api/auth/callback/discord
#
# 5. สำหรับ Omise ใช้ Live Keys ใน Production:
#    OMISE_PUBLIC_KEY="pkey_live_xxxxxxxxxxxxx"
#    OMISE_SECRET_KEY="skey_live_xxxxxxxxxxxxx"
#
# ดูคู่มือเพิ่มเติมได้ที่: VERCEL_DEPLOY.md
